interface:
  display_name: "Qwendbc"
  short_description: "Repo-specific patterns and workflows for qwendbc"
  default_prompt: "Use the qwendbc repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true